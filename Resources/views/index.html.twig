{% extends '@UxAdmin/main.html.twig' %}

{% block page_body %}
	<!-- begin #page-container -->
	<div id="page-container" class="page-container fade page-sidebar-fixed page-header-fixed">
		
{{ include('@UxAdmin/header.html.twig') }}

{{ include('@UxAdmin/sidebar.html.twig') }}
		
		<!-- begin #content -->
		<div id="content" class="content"></div>
		<!-- end #content -->

{{ include('@UxAdmin/theme-panel.html.twig') }}
	</div>
	<!-- end page container -->
{% endblock %}

{% block page_footer %}
	{{ parent() }}
	<script>
		App.setPageTitle('Hello World!');
		App.setPageOption({
			pageSidebarWide: true,
			//clearOptionOnLeave: true
		});
		//App.restartGlobalFunction();
		App.settings({
			ajaxMode: true,
			ajaxDefaultUrl: '#pages/index.html',
			ajaxType: 'GET',
			ajaxDataType: 'html'
		});
	</script>
{% endblock %}
